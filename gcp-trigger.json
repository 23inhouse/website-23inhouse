{
  "triggerTemplate": {
    "projectId": "${GCP_PROJECT_ID}",
    "repoName": "${GCP_GITHUB_REPO}",
    "branchName": "master"
  },
  "description": "${GCP_GITHUB_REPO}-deployment",
  "substitutions": {
    "_CLOUDSDK_COMPUTE_ZONE": "${GCP_COMPUTE_ZONE}",
    "_CLOUDSDK_CONTAINER_CLUSTER": "production"
  },
  "filename": "cloudbuild.yaml"
}
