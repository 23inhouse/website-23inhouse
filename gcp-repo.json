{
  "mirrorConfig": {
    "deployKeyId": "${GITHUB_KEY}",
    "url": "ssh://git@github.com/${GITHUB_USER}/${GITHUB_REPO}.git",
    "webhookId": "${GITHUB_HOOK}"
  },
  "name": "projects/${GCP_PROJECT}/repos/${GCP_GITHUB_REPO}"
}
